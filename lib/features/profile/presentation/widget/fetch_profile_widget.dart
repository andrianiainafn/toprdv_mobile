import 'package:flutter/material.dart';

class FetchProfileWidget extends StatelessWidget {
  const FetchProfileWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
